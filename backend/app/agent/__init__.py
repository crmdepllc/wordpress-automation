"""LangGraph agent package.

Sprint 1 contains a single ``ping`` node — an empty agent loop that calls
Claude and returns text, with no WordPress tooling yet. Later sprints add
skill nodes, the orchestration graph, and the human approval gate.
"""
